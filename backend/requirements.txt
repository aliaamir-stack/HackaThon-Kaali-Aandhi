# Core
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9   # for FastAPI file uploads

# AI / LLM
groq>=0.9.0               # Whisper + Llama + DeepSeek-R1

# State / validation
pydantic>=2.7.0

# LangGraph (Person B)
langgraph>=0.1.0

# RAG (Person D)
faiss-cpu>=1.8.0
langchain>=0.2.0
langchain-community>=0.2.0

# Utilities
python-dotenv>=1.0.0
